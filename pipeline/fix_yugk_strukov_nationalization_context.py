# -*- coding: utf-8 -*-
"""Месячная очередь, 8 сентября 2026 — карточка `ceeb8c214` (продажа 22%
акций ПАО «ЮГК» Газпромбанку у Константина Струкова, декабрь 2024).

НАХОДКА: у оставшейся после этой сделки доли Струкова (67,85%) есть
огромное продолжение, которого в базе нет вовсе — 11 июля 2025 года
Советский районный суд Челябинска по иску Генпрокуратуры обратил в доход
государства акции ПАО «ЮГК», принадлежавшие Струкову, и активы ещё
десяти связанных с ним компаний. Личный WebFetch (kommersant.ru/
doc/8317772) подтвердил дословно: «Генпрокуратура РФ подала иск... Заявление
поступило 2 июля 2025 года. Советский районный суд Челябинска удовлетворил
требования 11 июля»; «Активы ЮГК и 10 связанных с господином Струковым
компаний обратили в доход государства. Их стоимость оценивают в 220 млрд
руб.»; тот же источник прямо говорит, что доля Газпромбанка (АО «ААА
Управление капиталом» — покупатель по ЭТОЙ карточке) в исках о
национализации НЕ упоминается затронутой стороной — она остаётся
миноритарным акционером.

Это отдельный, гораздо более крупный сюжет (государственное изъятие, а не
коммерческая продажа) — не апдейт к этой карточке, а кандидат на
собственную. Здесь — только контекстная строка о судьбе оставшейся доли,
без изменения `buyer`/`seller`/`status`/`sum` самой карточки (они по-
прежнему верно описывают сделку декабря 2024 года). Решение о новой
карточке — за приточной рутиной; запись сделана в «Известные проблемы»
CLAUDE.md.

Поле `eco.context` уже прошло вычитку (`proofread_absorbed`) — за основу
слияния взят ТЕКУЩИЙ (уже вычитанный) текст с диска, а не старое значение
записи FIXES в batch_b_2024.py.

Запуск:
    python3 pipeline/fix_yugk_strukov_nationalization_context.py            # сухой прогон
    python3 pipeline/fix_yugk_strukov_nationalization_context.py --write    # запись
"""
import json
import sys

DATA = 'static/data/deals_promoted.json'
CARD_ID = 'ceeb8c214'

OLD_CONTEXT = (
    'После сделки Струков остаётся крупнейшим акционером ЮГК с долей '
    '67,85%, объём акций в свободном обращении сохраняется на уровне '
    '10,15%. Полученные средства пойдут на погашение долговых '
    'обязательств связанных сторон.'
)
NEW_CONTEXT = OLD_CONTEXT + (
    ' Позже, 11 июля 2025 года, Советский районный суд Челябинска по иску '
    'Генпрокуратуры обратил в доход государства акции ПАО «ЮГК», '
    'принадлежавшие Струкову, и активы ещё десяти связанных с ним '
    'компаний общей оценочной стоимостью 220 млрд ₽; миноритарная доля '
    'АО «ААА Управление капиталом» (Газпромбанк) в иске не затронута.'
)


def main(write=False):
    with open(DATA, encoding='utf-8') as f:
        data = json.load(f)
    by_id = {d['id']: d for d in data['deals']}
    card = by_id[CARD_ID]

    assert card['eco']['context'] == OLD_CONTEXT, \
        'eco.context уже другой: %r' % (card['eco']['context'],)

    print('eco.context: добавляется факт о национализации доли Струкова '
          '(июль 2025, Коммерсантъ doc/8317772)')

    if not write:
        print('Сухой прогон. Запись — с ключом --write.')
        return

    card['eco']['context'] = NEW_CONTEXT

    with open(DATA, 'w', encoding='utf-8') as f:
        json.dump(data, f, ensure_ascii=False, indent=1)
    print('Записано.')


if __name__ == '__main__':
    main('--write' in sys.argv)
