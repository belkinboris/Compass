# -*- coding: utf-8 -*-
"""Месячная очередь (REVISION_BRIEF), карточка g5f24bb26 (IPO ЕвроТранс/
«ТРАССА»): дельта-поиск дописывает eco.fin (только что заполненный
review.py фактом обвала акций одной цитатой) фактами из ДРУГИХ
источников — исками кредиторов и жалобой в Банк России на манипулирование
рынком, поданной ассоциацией владельцев облигаций. Это не повод для
review.py: три разных источника в одном поле, дословность проверяется по
одной цитате.

Источники: smart-lab.ru (блог T-Invest, иски кредиторов; блог
«Академия банкротства», жалоба в ЦБ) — оба читал напрямую.
"""

import json
import os

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
PATH = os.path.join(ROOT, 'static', 'data', 'deals_promoted.json')

CARD_ID = 'g5f24bb26'

OLD_FIN = (
    'акции «Евротранса» в начале биржевых торгов рухнули на 11%, до '
    '103,25 руб., обновив минимум с декабря 2024 года.'
)
FIN_ADDITION = (
    'На фоне техдефолта по облигациям в 2026 году к компании выстроилась '
    'очередь исков от кредиторов: только в январе–марте восемь компаний '
    'подали иски на общую сумму около 12 млрд рублей (в том числе «Фьюел '
    'Менеджмент АЗС» — 3,02 млрд рублей к «Трасса ГСМ» и 7,4 млрд рублей '
    'к самому ПАО); в компании называли претензии необоснованными. '
    'Ассоциация владельцев облигаций официально обратилась в Банк России '
    'с жалобой на то, что ПАО «ЕвроТранс» систематически нарушало закон '
    'об инсайдерской информации и манипулировании рынком.'
)
NEW_FIN = OLD_FIN + ' ' + FIN_ADDITION


def main(write=False):
    data = json.load(open(PATH, encoding='utf-8'))
    deal = next(d for d in data['deals'] if d['id'] == CARD_ID)

    assert deal['eco']['fin'] == OLD_FIN, \
        f"eco.fin: неожиданное значение {deal['eco']['fin']!r}"

    print(f"{CARD_ID} eco.fin: += иски кредиторов и жалоба в ЦБ на "
          "манипулирование рынком (2026 год)")
    deal['eco']['fin'] = NEW_FIN

    if write:
        with open(PATH, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=1)
        print("ЗАПИСАНО")
    else:
        print("Сухой прогон. Запись — с --write.")


if __name__ == '__main__':
    import sys
    main(write='--write' in sys.argv)
