# -*- coding: utf-8 -*-
"""Месячная очередь (REVISION_BRIEF, третий уровень), карточка
`g5eea9928` («ГК "Урбантех" приобрела 55% в ООО "Технологии
безопасности дорожного движения"», 2023, Закрыта) — продавец не был
назван, дальнейший рост предмета сделки не прослеживался.

Проверено ЛИЧНО прямым WebFetch (через читалку-прокси r.jina.ai —
tadviser.ru отдаёт WebFetch напрямую 404, но текст оригинальный):
tadviser.ru/index.php/Компания:Технологии_безопасности_дорожного_движения_(ТБДД):
- «55-процентная доля в уставном капитале ТБДД перешла от теперь уже
  бывшего совладельца компании Виктора Ардашева», сделка проведена
  через «МВС Груп», входящую в группу компаний «Урбантех»;
- 2022 год: выручка 572,9 млн ₽, чистая прибыль 118 млн ₽;
- 2023 год: выручка 915,8 млн ₽, чистая прибыль 240,2 млн ₽;
- «В 2023 году компания почти двукратно — до 1353 единиц — увеличила
  объем выпускаемого оборудования» (годом ранее — 702).

НЕ ВНЕСЕНО: (1) официальная сумма сделки — пресс-служба «Урбантеха»
прямо отказалась её называть (та же статья ComNews, что уже в `src`);
(2) расхождение состава учредителей на 2026 год (list-org.com: МВС
Груп 55%, Сычиков 40%, Александр Вавилин 5% — по старым статьям у
Сычикова было 45%) — не проверено, откуда взялась третья доля, не
вносится без отдельной проверки; (3) новые контракты ГРУППЫ «Урбантех»
целиком (концессии в Дагестане, Нижегородской области) — это про всю
группу, не про предмет карточки (ТБДД) отдельно; (4) консультанты
сделки и согласование ФАС — не найдены нигде.

Запуск: python3 pipeline/fix_urbantech_tbdd_seller_and_growth.py
        python3 pipeline/fix_urbantech_tbdd_seller_and_growth.py --write
"""
import json
import os
import sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
PATH = os.path.join(ROOT, 'static', 'data', 'deals_promoted.json')

DEAL_ID = 'g5eea9928'

OLD_ECO_CONTEXT = (
    'Группа компаний «Урбантех» объединяет разработчиков и '
    'интеграторов цифровых решений для транспортной отрасли, а также '
    'операторов технологической инфраструктуры на транспорте. Среди '
    'её проектов — крупнейшая в России концессия по безопасности '
    'дорожного движения в Московской области, а также интеллектуальные '
    'транспортные системы в Нижегородской области и Республике '
    'Дагестан.'
)
NEW_ECO_CONTEXT = (
    OLD_ECO_CONTEXT + ' Сделка проведена через входящую в «Урбантех» '
    '«МВС Груп», выкупившую долю у прежнего совладельца ТБДД Виктора '
    'Ардашева. После сделки компания заметно выросла: выручка '
    'поднялась с 572,9 млн ₽ в 2022 году до 915,8 млн ₽ в 2023-м '
    '(чистая прибыль — 118 и 240,2 млн ₽ соответственно), а выпуск '
    'комплексов «Азимут» почти удвоился — до 1353 единиц в 2023 году '
    'против 702 годом ранее.'
)

OLD_SRC = [['ComNews', 'https://www.comnews.ru/content/223993/2023-01-20/2023-w03/urbantekh-ukreplyaet-pozicii']]
NEW_SRC = OLD_SRC + [['TAdviser', 'https://www.tadviser.ru/index.php/Компания:Технологии_безопасности_дорожного_движения_(ТБДД)']]


def main(write=False):
    with open(PATH, encoding='utf-8') as f:
        data = json.load(f)
    deal = next(d for d in data['deals'] if d['id'] == DEAL_ID)

    assert deal['eco']['context'] == OLD_ECO_CONTEXT
    assert deal['src'] == OLD_SRC

    print('=== eco.context: станет ===')
    print(NEW_ECO_CONTEXT)
    print('\n=== src: станет ===')
    print(NEW_SRC)

    if write:
        deal['eco']['context'] = NEW_ECO_CONTEXT
        deal['src'] = NEW_SRC
        with open(PATH, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=1)
        print('\nЗаписано.')
    else:
        print('\nСухой прогон — ничего не записано. Запустите с --write.')


if __name__ == '__main__':
    main(write='--write' in sys.argv)
