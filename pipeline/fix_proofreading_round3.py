# -*- coding: utf-8 -*-
"""Вычитка, круг 3: устаревшие статусы «Обсуждается» — три из трёх
проверенных оказались фактически завершёнными историями (15 августа 2026,
проверка живым поиском + curl; цитаты и адреса — ниже по карточкам).

Замер по базе: карточек со статусом «Обсуждается» и датой до 2025 года —
121. Здесь чинятся три, попавшие в выборку глубокой вычитки; результат
3/3 устаревших — сигнал, что класс A3 каталога (PROOFREADING_CATALOG.md)
надо проходить целиком.

g321bdead (Эталон / «Бизнес-Недвижимость») -> ПОДПИСАНА, дата 28.08.2025.
  Интерфакс (28.08.2025) и mergers.ru (29.08.2025): соглашение о покупке
  100% акций заключено 28 августа 2025. Ведомости (20.02.2026): «привлекла
  18,4 млрд руб. по итогам SPO… Из полученных средств 14,1 млрд руб.
  направят на финансирование стратегической сделки по приобретению
  АО «Бизнес-недвижимость»». Прямой публикации «оплата завершена» нет,
  поэтому честный статус — «Подписана», не «Закрыта». Дата карточки была
  голым годом «2025»; день и месяц названы источниками — уточнение внутри
  того же года. Плюс класс A6: продавец АФК «Система» текстом при живом
  профиле gc2792a44 — привязан seller_id.

g9180c0a6 (ВЭБ.РФ / префы РЖД на 82 млрд) -> НЕ СОСТОЯЛАСЬ.
  План завис в декабре 2021 («В РЖД допустили перенос сделки по продаже
  доли ВЭБу», РБК 14.12.2021); в 2022 капитал РЖД пополнили иначе — «Весь
  объем "префов" на 217 млрд рублей будет размещен по закрытой подписке в
  пользу единственного акционера — Российской Федерации» (ТАСС,
  02.12.2022, пересказ Investfunds), за счёт ФНБ. Контроль на сегодня:
  «Все акции компании принадлежат государству» (Ведомости, 02.06.2026) —
  ВЭБ.РФ акционером РЖД не стал.

g7632fe9f (Skillbox / Grafika) -> ЗАКРЫТА (10% куплены в 2022), с
  пост-историей: RB.RU (08.08.2024): «Skillbox вышел из числа учредителей
  школы рисования Grafika. Холдингу там принадлежало 10%… Покупателем доли
  выступил гендиректор ООО «Творческое образование»… Андрей Ломтев…
  до сделки ему… принадлежало 47,37%, с 1 августа 2024 года — 57,37%…
  Представитель холдинга подтвердил RB.RU факт сделки». То есть покупка
  2022 года состоялась (Skillbox реально числился учредителем с 10%), план
  «нарастить долю в 2023-м» реализован не был, в 2024-м доля продана
  обратно. Выход 2024 года — ОТДЕЛЬНОЕ событие (кандидат на свою карточку,
  задача притока), здесь — только честный финал истории в контексте.
  Заголовок и extra переведены из будущего времени в прошедшее (правило
  согласования времени со статусом); eco.share, дословно повторявшая
  заголовок-фразу, заменена на предмет.

Запуск:
    python3 pipeline/fix_proofreading_round3.py            # сухой прогон
    python3 pipeline/fix_proofreading_round3.py --write    # запись
"""
import json
import os
import sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
DATA = os.path.join(ROOT, 'static', 'data', 'deals_promoted.json')

SETS = [
 ('g321bdead', 'status', 'Обсуждается', 'Подписана'),
 ('g321bdead', 'date', '2025', '2025-08-28'),
 ('g321bdead', 'seller_id', None, 'gc2792a44'),

 ('g9180c0a6', 'status', 'Обсуждается', 'Не состоялась'),

 ('g7632fe9f', 'status', 'Обсуждается', 'Закрыта'),
 ('g7632fe9f', 'title',
  'Skillbox купит 10% сети школ рисования Grafika',
  'Skillbox купил 10% сети школ рисования Grafika'),
 ('g7632fe9f', 'extra',
  'Стратегическое приобретение: Skillbox планирует купить 10% сети школ '
  'рисования Grafika до конца 2022 года и нарастить долю в 2023 году. '
  'Компании создадут совместное предприятие для развития франшизы '
  'офлайн-школ в регионах России и странах СНГ.',
  'Стратегическое приобретение: Skillbox купил 10% сети школ рисования '
  'Grafika в конце 2022 года; план нарастить долю в 2023 году реализован '
  'не был. Компании создали совместное предприятие для развития франшизы '
  'офлайн-школ в регионах России и странах СНГ.'),
 ('g7632fe9f', 'eco.share',
  'Образовательная платформа Skillbox до конца 2022 года купит 10% сети школ '
  'рисования для взрослых Grafika и планирует нарастить свою долю в 2023-м, '
  'рассказали в компании.',
  '10% ООО «Творческое образование» (сеть школ рисования Grafika).'),
]

APPENDS = [
 ('g321bdead', 'eco.context',
  'Соглашение о приобретении 100% акций АО «Бизнес-Недвижимость» заключено '
  '28 августа 2025 года. SPO завершено в феврале 2026 года: привлечено '
  '18,4 млрд руб., из которых 14,1 млрд руб. направляются на оплату сделки.'),
 ('g9180c0a6', 'eco.context',
  'Сделка не была реализована: в 2022 году капитал РЖД пополнили другим '
  'механизмом — весь объём привилегированных акций на 217 млрд руб. размещён '
  'по закрытой подписке в пользу единственного акционера, Российской '
  'Федерации (за счёт средств ФНБ), а в апреле 2022 года правительство '
  'докапитализировало холдинг на 250 млрд руб. ВЭБ.РФ акционером РЖД не стал.'),
 ('g7632fe9f', 'eco.context',
  'История завершилась выходом: 1 августа 2024 года, по данным ЕГРЮЛ, '
  'Skillbox продал свои 10% гендиректору ООО «Творческое образование» Андрею '
  'Ломтеву, доля которого выросла с 47,37% до 57,37%.'),
]

NEW_SRC = [
 ('g321bdead', ['Интерфакс', 'https://www.interfax.ru/business/1044560']),
 ('g321bdead', ['Ведомости',
  'https://www.vedomosti.ru/investments/news/2026/02/20/1177921-etalon-privlekla']),
 ('g9180c0a6', ['ТАСС (пересказ Investfunds)', 'https://investfunds.ru/news/160484/']),
 ('g7632fe9f', ['RB.RU', 'https://rb.ru/news/skillbox-grafika-deal/']),
]


def get_field(card, path):
    obj = card
    for part in path.split('.'):
        if not isinstance(obj, dict):
            return None
        obj = obj.get(part)
    return obj


def set_field(card, path, value):
    parts = path.split('.')
    obj = card
    for part in parts[:-1]:
        obj = obj.setdefault(part, {})
    obj[parts[-1]] = value


def main(argv):
    data = json.load(open(DATA, encoding='utf-8'))
    by_id = {d['id']: d for d in data['deals']}

    for cid, field, old, _new in SETS:
        current = get_field(by_id[cid], field)
        assert current == old, \
            '%s.%s: поле уже другое: %r, ожидали %r' % (cid, field, str(current)[:60], str(old)[:60])
    for cid, url_pair in NEW_SRC:
        urls = {s[1] for s in by_id[cid].get('src') or [] if len(s) > 1}
        assert url_pair[1] not in urls, '%s: источник уже добавлен' % cid

    print('Правок: %d присвоений, %d дописок, %d источников'
          % (len(SETS), len(APPENDS), len(NEW_SRC)))
    for cid, field, old, new in SETS[:6]:
        print('  %s %-9s %r -> %r' % (cid, field, str(old)[:34], str(new)[:40]))

    if '--write' not in argv:
        print('\nСухой прогон. Запись — с ключом --write.')
        return 0

    for cid, field, _old, new in SETS:
        set_field(by_id[cid], field, new)
    for cid, field, text in APPENDS:
        current = get_field(by_id[cid], field)
        if isinstance(current, str) and current.strip() not in ('', '—'):
            set_field(by_id[cid], field, current.rstrip() + ' ' + text)
        else:
            set_field(by_id[cid], field, text)
    for cid, url_pair in NEW_SRC:
        by_id[cid].setdefault('src', []).append(url_pair)

    with open(DATA, 'w', encoding='utf-8') as f:
        json.dump(data, f, indent=1, ensure_ascii=False)
    print('ЗАПИСАНО.')
    return 0


if __name__ == '__main__':
    sys.exit(main(sys.argv[1:]))
