# -*- coding: utf-8 -*-
"""Месячная очередь (REVISION_BRIEF, третий уровень), карточка
`gde280a8d` («УК "Современные фонды недвижимости" выкупила логопарк
PNK Park Медведково», закрыта, 2023-03-13) — не было названо, в какой
именно фонд вошёл актив, и не было общей бизнес-модели продавца.

Проверено ЛИЧНО прямым WebFetch (дословная цитата,
all-pribors.ru/opisanie/91993-24 — реестр средств измерений):
«Система автоматизированная информационно-измерительная коммерческого
учета электроэнергии (АИИС КУЭ) ООО "СФН" Д.У. Комбинированным ЗПИФ
"Современная Коммерческая недвижимость" (ЦОД Медведково)» — то есть
логопарк учтён как объект именно этого фонда.

По докладу саб-агента (WebSearch-сводка, sfn-am.ru дважды отдал 503 —
не проверено дословным чтением самого сайта СФН): в портфеле фонда
актив описан как «Центр обработки данных / Складской комплекс в
Москве» площадью 73,8 тыс. кв. м, арендаторы — СДЭК и ЦХД; СЧА фонда на
февраль 2026 — 98,5 млрд ₽. Эти цифры НЕ внесены в карточку — сайт
управляющей компании недоступен для прямой проверки, а цена одной
косвенной регистрационной записи (реестр измерений) не заменяет
дословное чтение первоисточника управляющей компании.

НЕ ВНЕСЕНО (важное предупреждение против путаницы): в августе 2024
года «Ростелеком» купил ДРУГОЙ дата-центр — «Медведково 2» — за 26,28
млрд ₽, но это актив СОВСЕМ ДРУГОГО фонда, «Рентал ПРО» (УК «А класс
капитал»), не СФН и не PNK Park (kommersant.ru/doc/6892734) — простое
совпадение района Москвы, не имеет отношения к этой сделке; при
дочитывании этот сюжет специально отсеян, чтобы не спутать два разных
объекта. Также не внесены: официальная сумма сделки (ноль, только
оценка эксперта Новикова, уже в карточке); консультанты (ноль);
причина продажи именно этого актива со стороны PNK Group (ноль —
известна только общая бизнес-модель компании «построил — продал под
ЗПИФ», практикуемая с 2016 года, не привязанная к конкретной сделке).

Запуск: python3 pipeline/fix_sfn_pnk_medvedkovo_fund.py
        python3 pipeline/fix_sfn_pnk_medvedkovo_fund.py --write
"""
import json
import os
import sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
PATH = os.path.join(ROOT, 'static', 'data', 'deals_promoted.json')

DEAL_ID = 'gde280a8d'

OLD_LAW_STRUCT = '—'
NEW_LAW_STRUCT = (
    'По данным реестра средств измерений, логопарк учтён как объект '
    'комбинированного ЗПИФ «Современная Коммерческая недвижимость» '
    'под управлением СФН.'
)

OLD_SRC = [['Коммерсантъ', 'https://www.kommersant.ru/doc/5873533']]
NEW_SRC = OLD_SRC + [
    ['Реестр средств измерений', 'https://all-pribors.ru/opisanie/91993-24'],
]


def main(write=False):
    with open(PATH, encoding='utf-8') as f:
        data = json.load(f)
    deal = next(d for d in data['deals'] if d['id'] == DEAL_ID)

    assert deal['law']['struct'] == OLD_LAW_STRUCT
    assert deal['src'] == OLD_SRC

    print('=== law.struct: станет ===')
    print(NEW_LAW_STRUCT)
    print('\n=== src: станет ===')
    print(NEW_SRC)

    if write:
        deal['law']['struct'] = NEW_LAW_STRUCT
        deal['src'] = NEW_SRC
        with open(PATH, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=1)
        print('\nЗаписано.')
    else:
        print('\nСухой прогон — ничего не записано. Запустите с --write.')


if __name__ == '__main__':
    main(write='--write' in sys.argv)
