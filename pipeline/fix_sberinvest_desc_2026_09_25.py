# -*- coding: utf-8 -*-
"""Качество, 25 сентября 2026 (ежедневный прогон 21:37 МСК) — очередь находок
аудита (PROFILE_DESC_IS_EVENT), профиль компании `gac30cf97` («СберИнвест»).
Описание называло её «инвестиционно-банковским подразделением Сбербанка» —
по TAdviser это компания прямых/мезонинных инвестиций, а не классический
инвестбанкинг: она сама входит в капитал чужих активов (Медскан, «Пивоварни
Бочкарёв», «Аггро», «Грушевские сады» — все в списке TAdviser), а не
консультирует и организует размещения для клиентов.

Дословная цитата (tadviser.ru, страница «СберИнвест (Сбербанк Инвестиции)»):
«СберИнвест (ООО «Сбербанк Инвестиции») — 100-процентная дочерняя компания
Сбербанка, входящая в блок «Корпоративно-инвестиционный бизнес». Ключевое
направление деятельности компании — предоставление клиентам банка
финансовых решений и структурирование сделок через акционерные и
мезонинные инструменты».

Запуск:
    python3 pipeline/fix_sberinvest_desc_2026_09_25.py            # сухой прогон
    python3 pipeline/fix_sberinvest_desc_2026_09_25.py --write    # запись
"""
import json
import sys

DATA_PATH = 'static/data/deals_promoted.json'

OLD_DESC = 'Инвестиционно-банковское подразделение Сбербанка.'
NEW_DESC = ('100-процентная дочерняя компания Сбербанка, структурирует '
            'сделки клиентов через акционерные и мезонинные инструменты.')


def main(write):
    data = json.load(open(DATA_PATH, encoding='utf-8'))
    company = data['companies']['gac30cf97']
    assert company['desc'] == OLD_DESC, repr(company['desc'])
    company['desc'] = NEW_DESC

    if write:
        json.dump(data, open(DATA_PATH, 'w', encoding='utf-8'), ensure_ascii=False, indent=1)
        print('Описание gac30cf97 исправлено по TAdviser. ЗАПИСАНО.')
    else:
        print('Сухой прогон: поправил бы описание. Повторите с --write.')


if __name__ == '__main__':
    main(write='--write' in sys.argv)
