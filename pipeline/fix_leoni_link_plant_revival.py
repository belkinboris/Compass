# -*- coding: utf-8 -*-
"""Месячная очередь (REVISION_BRIEF, третий уровень), карточка
g1e73548b (Leoni продала завод электрожгутов ООО «Импульс») — судьба
завода после сделки (возобновление производства, новые заказчики,
финансы) не была отражена. Проверено лично прямым WebFetch трёх
источников.

`eco.context` (заполнено, было «—»). Завод переименован и возобновил
работу. Дословно (Интерфакс): «ООО "Завод Линк" (ранее — ООО "Леони
Рус", г. Заволжье)»; «Проектная мощность производства – 300 тыс.
машинокомплектов в год, выйти на нее планируется не ранее 2025 года.
До конца этого года на заводе планируется выпустить 50 тыс.
машинокомплектов». Новые заказчики взамен утраченного Renault:
дословно (Коммерсантъ): «Пока предприятие сотрудничает с "АвтоВАЗом" и
изготавливает жгуты проводов для модельного ряда автомобилей "Лада"»;
«компания разрабатывает жгуты для электромобиля "Атом". Поставки
планируется начать в 2025 году»; «Ведутся переговоры с
белорусско-китайским предприятием "Белджи" и автомобильным заводом
"Урал"». Финансовый результат за 2025 год — дословно (audit-it.ru):
«выручку в сумме 3,5 млрд руб., что на... 25,1%, больше, чем годом
ранее», «прибыль в размере 170 млн руб. Это на 27,6% больше, чем в
2024 г.».

НЕ ВКЛЮЧЕНО: сумма сделки — Коммерсантъ прямо пишет «Сумма сделки не
раскрывалась», независимой оценки не найдено (историческая цифра
инвестиций Leoni в завод, 1,47 млрд ₽, — не сумма ЭТОЙ сделки, а
прежние вложения продавца, другая величина). Другие активы Дениса и
Ильи Черневых (ООО «Автопромжгут») — не относятся к условиям ЭТОЙ
сделки, не переносятся. Консультанты сделки — не найдены ни в одном
источнике.

Запуск: python3 pipeline/fix_leoni_link_plant_revival.py
        python3 pipeline/fix_leoni_link_plant_revival.py --write
"""
import json
import os
import sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
PATH = os.path.join(ROOT, 'static', 'data', 'deals_promoted.json')

DEAL_ID = 'g1e73548b'

OLD_CONTEXT = '—'
NEW_CONTEXT = (
    'Завод переименован в ООО «Завод Линк» и возобновил производство: '
    'проектная мощность — 300 тыс. машинокомплектов в год (выход на неё '
    'ожидается не ранее 2025 года, план на первый год — 50 тыс.). Новые '
    'заказчики взамен утраченного Renault — «АвтоВАЗ» (жгуты для '
    'модельного ряда «Лада»), разработка жгутов для электромобиля '
    '«Атом» (поставки с 2025 года), переговоры с «Белджи» и '
    'автозаводом «Урал». Выручка за 2025 год выросла на 25,1%, до 3,5 '
    'млрд ₽, чистая прибыль — на 27,6%, до 170 млн ₽.'
)

NEW_SRC = [
    ['Интерфакс', 'https://www.interfax.ru/business/969496'],
    ['Коммерсантъ', 'https://www.kommersant.ru/doc/6818888'],
    ['audit-it.ru', 'https://www.audit-it.ru/contragent/1075248001652_ooo-zavod-link'],
]


def main(write=False):
    with open(PATH, encoding='utf-8') as f:
        data = json.load(f)
    deal = next(d for d in data['deals'] if d['id'] == DEAL_ID)

    assert deal['eco']['context'] == OLD_CONTEXT, deal['eco']['context']
    for label, url in NEW_SRC:
        assert not any(s[1] == url for s in deal['src']), f'{url} уже в src'

    print('=== eco.context: станет ===')
    print(NEW_CONTEXT)
    print('\n=== src добавится ===')
    for s in NEW_SRC:
        print(s)

    if write:
        deal['eco']['context'] = NEW_CONTEXT
        deal['src'].extend(NEW_SRC)
        with open(PATH, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=1)
        print('\nЗаписано.')
    else:
        print('\nСухой прогон — ничего не записано. Запустите с --write.')


if __name__ == '__main__':
    main(write='--write' in sys.argv)
