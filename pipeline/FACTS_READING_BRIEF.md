# Чтение фактов сделки — инструкция читателю

Вы подтверждаете ФАКТЫ, по которым платформа считает числа: цену сделки,
дату, долю, природу сделки и периметр. Не описание, не пересказ — только
ответы на вопросы ниже, каждый с дословной цитатой из источника. Два читателя
работают независимо, их ответы сверяются машинно: факт становится
«подтверждённым», только если оба назвали одно и то же значение и один и тот
же смысл. Поэтому не додумывайте: если источник не устанавливает факт,
ответ `null` — это тоже результат.

## Что дано

Файл заданий (JSON-список): для каждой сделки `id`, заголовок, дата, статус,
тип, сумма и текст поля «Предмет / доля» карточки, покупатель, продавец,
профиль предмета и его ИНН, список источников (`sources`) и список фактов,
которые нужно подтвердить (`confirm`). У заданий на периметр есть ещё
`report`: юрлицо, ИНН, год и выручка отчёта, который пойдёт в знаменатель.

Тексты источников уже скачаны в кэш: `python3 pipeline/facts_source_text.py <url>`
печатает текст статьи. Если адреса в кэше нет, откройте его WebFetch. Если
источник не открывается никак — так и напишите (`"unreadable": true` для
сделки, если не открылся ни один).

Читайте ВСЕ источники сделки, а не первый попавшийся: цена может стоять в
одном, дата закрытия в другом.

## Вопросы (по каждому — значение + дословная цитата + адрес источника)

1. **price** — сумма сделки.
   - `value_rub` — число в рублях (1,8 млрд ₽ → 1800000000). Если сумма в
     валюте без рублёвого эквивалента в источнике — `null`.
   - `meaning` — КАК раскрыта сумма, одно из:
     `disclosed` (цена названа сторонами, официальным сообщением, документом
     или реестром), `reported` (число есть, но его называют анонимные
     «источники» издания: «по данным источников РБК», «как стало известно
     Ъ»), `estimate` (оценка экспертов, «около», «~»), `range` (диапазон),
     `lower_bound` («более», «не менее»), `auction_start` (стартовая или
     запрашиваемая цена, «выставлен на продажу за»), `foreign_currency`,
     `not_a_price` (число есть, но это не цена сделки: иск, объём инвестиций
     в проект, докапитализация, списание, размер фонда, кредит), `valuation`
     (оценка всей компании при размещении), `raise` (объём привлечения),
     `undisclosed` (источники цену не называют).
   - `attribution` — КТО назвал число, обязательно: `parties` (стороны,
     пресс-релиз, официальное заявление), `filing` (отчётность, проспект,
     раскрытие, документы сделки), `registry` (ЕГРЮЛ, реестр торгов),
     `adviser` (число подал консультант стороны — в таблицу «Сделки года»
     издания, в рейтинг, в своё объявление, — а сама сторона его не
     называла), `media_sources` (анонимные источники издания), `analyst`
     (оценщик, аналитик), `unknown`. `disclosed` допустим только при
     `parties`, `filing` или `registry`; при `adviser` и `media_sources`
     ставьте `reported`.
   - `event` — к какому событию относится цена: `closing`, `signing`,
     `announcement`, `publication`, `registry`. Если в карточке смешаны две
     разные сделки с одинаковой суммой (разрешение одному покупателю, потом
     продажа другому), назовите событие и покупателя в `notes`.
   - `scope` — ЗА ЧТО эта цена: `package` (за купленный пакет), `equity`
     (вся компания, 100% капитала), `ev` (с учётом долга, enterprise value),
     `unknown`.
   - `terms` — СОСТАВ цены: `fixed` (твёрдая сумма), `includes_contingent`
     (в сумму входит условная или отложенная часть, оценённая на дату
     покупки: «640 млн ₽ отложенного возмещения и 114 млн ₽ условного
     возмещения» у SmartDeal), `paid` (фактически выплаченная сумма),
     `unknown`. Если источник называет части — перечислите их в `notes`.
   - `attribution_quote` — вторая дословная цитата ИЗ ТОГО ЖЕ текста, из
     которой видно, КТО назвал число («сообщила пресс-служба Альфа-банка»,
     «говорится в отчётности», «по итогам аукциона»). Нужна всегда, когда
     `attribution` — `parties`, `filing` или `registry`, а в основной цитате
     нет слов «сообщил / заявил / говорится / пресс-служба / отчётность /
     протокол / распоряжение / реестр». Проверка без неё отказывает.
   - ТОЧНОСТЬ. Значение обязано быть ровно таким, как в цитате: цитата
     «50,8 млрд руб.» подтверждает только 50 800 000 000, а не 50 785 000 000.
     Нужна точная цифра — найдите цитату с точной цифрой (сообщение ТАСС со
     ссылкой на пресс-службу, релиз компании), иначе пишите значение в
     точности цитаты. Проверка отказывает, если значение точнее цитаты.
   - Пример ловушки: «500 млрд ₽» стояли в статье и относились к
     допэмиссии ВТБ, а не к цене партнёрства — это `not_a_price`. «Фонд
     объёмом до 30 млрд ₽» — не цена покупки, это `raise` или `not_a_price`.
2. **date** — дата события карточки.
   - `value` — `ГГГГ-ММ-ДД` (или `ГГГГ-ММ`, если день не назван).
   - `meaning` — что это за дата: `closing` (закрытие, переход прав),
     `signing` (подписание), `announcement` (объявление), `publication`
     (дата сообщения о закрытии, когда сам день закрытия не назван),
     `registry` (запись в ЕГРЮЛ).
   - Цитата должна содержать день и месяц словами («24 апреля») или дату.
3. **nature** — `control_change`: true, если в результате сделки
   КОНЕЧНЫЙ контроль над бизнесом переходит к другой группе (покупка
   компании, актива, контрольного пакета независимым покупателем); false
   для допэмиссии в пользу инвестора, buyback, финансирования, создания
   СП, реорганизации, покупки миноритарного пакета без контроля — и для
   передачи между структурами ОДНОЙ группы.
   - `intragroup` — true, если продавец и покупатель входят в одну группу
     или у них один конечный владелец (казахстанские активы передавались
     между структурами VEON, расчёт взаимозачётом): прямой держатель
     сменился, конечный контроль — нет. При `intragroup: true`
     `control_change` обязан быть false. Различайте прямого покупателя и
     конечную группу; если источник не даёт понять, кто конечный владелец
     покупателя, — `intragroup: false` и оговорка в `notes`.
   - Цитата и АДРЕС источника обязательны: чтение без адреса или с
     цитатой, которой нет в тексте, не считается подтверждением.
4. **stake** (только если есть в `confirm`) — доля, приобретаемая ИМЕННО в
   этой сделке, в процентах.
   - `value` — число; `object` — чьи это доли или акции (название юрлица или
     компании, как в источнике); `event` — к какому событию относится
     (`closing`, `signing`, `announcement`, `registry`).
   - Цитата должна содержать процент рядом со словом о покупке или продаже
     («приобрела 100% долей ООО «Убик»»), либо слова о покупке целиком
     («стал единственным владельцем»).
   - Не считать: «консолидировал 100%» (это итог, не покупка), «ранее владел
     30%», «первый этап — 68%», доли других участников консорциума. Если в
     сделке несколько этапов с разными процентами и сумма карточки относится
     ко всей сделке — доля всей сделки; если непонятно, `null`.
5. **perimeter** (только если есть в `confirm`) — можно ли делить цену на
   ЭТОТ отчёт из задания (`report`: юрлицо, ИНН, год, выручка).
   - `report` — скопируйте из задания (`inn`, `year`, `revenue_rub`,
     `legal_name`): читатель подтверждает периметр относительно конкретного
     отчёта, а не названия.
   - `covers_business`: true, только если куплено ИМЕННО это юрлицо (или его
     акции/доли) и его отдельная отчётность описывает купленный бизнес
     целиком. false, если куплен актив (здание, участок, бренд), холдинговая
     оболочка над группой, часть бизнеса, другое юрлицо, или если бизнес
     ведут несколько юрлиц (владелец имущества и управляющая компания,
     несколько операционных компаний) — тогда выручка одного из них не
     описывает купленное.
   - `other_entities` — список других юрлиц, входящих в сделку или ведущих
     тот же бизнес, по источникам (например, `["ООО «УК Камелия»"]`). Пустой
     список — если других нет.
   - `ok`: true только при `covers_business: true` и пустом `other_entities`.
   - `entity` — название юрлица из цитаты.
   - Сверьте порядок величины: цена сделки и выручка отчёта. Если выручка
     отчёта в разы меньше того, что источники говорят о бизнесе (группа
     заводов, сеть магазинов), периметр не подтверждён.

## Правила

- Цитата — ДОСЛОВНО из текста источника, одно-два предложения, не длиннее
  400 знаков, без правок и сокращений внутри. Она проверяется машинно на
  совпадение с текстом.
- Значение обязано выводиться из цитаты: процент, число с единицей, дата.
- Один источник на факт, самый прямой (официальное сообщение, ЕГРЮЛ,
  деловое издание). Не смешивайте цитаты из разных статей в одну.
- Не используйте знания о других сделках и общие представления; только этот
  источник.
- Сомневаетесь между двумя трактовками — `null` и объяснение в `notes`.
- Не меняйте карточку и не пишите ничего, кроме файла ответа.

## Формат ответа

JSON-список, по объекту на сделку из задания, в файл, указанный в задаче:

```json
[
 {"id": "gdf51a7ca", "reader": "A", "unreadable": false,
  "price": {"value_rub": 1814000000, "meaning": "disclosed", "attribution": "filing", "event": "closing", "scope": "equity",
            "terms": "includes_contingent",
            "quote": "Справедливая стоимость приобретения составляет 1,814 млрд руб.",
            "attribution_quote": "говорится в отчётности компании по МСФО", "source": "https://..."},
  "date": {"value": "2026-04-21", "meaning": "closing",
           "quote": "Сделка закрыта 21 апреля.", "source": "https://..."},
  "nature": {"control_change": true, "intragroup": false, "quote": "Arenadata приобрела 100% ООО «Убик»", "source": "https://..."},
  "stake": {"value": 100, "object": "ООО «Убик»", "event": "closing",
            "quote": "Arenadata приобрела 100% долей ООО «Убик»", "source": "https://..."},
  "perimeter": {"ok": true, "covers_business": true, "other_entities": [], "entity": "ООО «Убик»",
                "report": {"inn": "7704780110", "year": 2024, "revenue_rub": 595997000, "legal_name": "ООО «УБИК»"},
                "quote": "…разработчик… ООО «Убик» …", "source": "https://..."},
  "notes": "цена названа в пресс-релизе; долг не упоминается"}
]
```

Факт, которого источники не устанавливают, — `null` (например,
`"stake": null`). Если в задании `confirm` содержит только часть фактов,
заполняйте только их. Поле `reader` — ваша буква из задачи.
