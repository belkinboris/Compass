# -*- coding: utf-8 -*-
"""Месячная очередь (REVISION_BRIEF, третий уровень), карточка
`g4cb8fc20` («Росатом интересуется приобретением золотодобывающего
проекта ООО «Техсервис» в Иркутской области», «Обсуждается») — интерес
«Росатома» 2022 года ни разу больше не упоминается ни в одном источнике,
а актив в итоге продан совсем другому, не связанному покупателю.

Проверено ЛИЧНО прямым WebFetch (дословные цитаты):
- kommersant.ru/doc/5707294 (исходный источник карточки, повторно
  проверен): «Эльконский ГМК (структура «Росатома») планирует заказать
  оценку доли в уставном капитале ООО «Техсервис»» — Эльконский ГМК
  прямо назван структурой РОСАТОМА (не Ростеха); отдельно в тексте «35%
  в «Техсервисе» владеет структура «Ростеха» «РТ-Развитие бизнеса»» —
  это ДРУГАЯ госкорпорация, уже бывшая совладельцем актива на тот
  момент, а не потенциальным покупателем.
- gold.1prime.ru/20251009/606780.html (09.10.2025): «Структура «Ростеха»
  и ее партнеры продали ООО «Техсервис»... В октябре 100% «Техсервиса»
  получило АО «ИГ Геоинвест»»; «До сделки «РТ-Развитие бизнеса» владела
  35% «Техсервиса»... доля общества пока находится в залоге у входящего
  в «Ростех» ООО «РТ-Развитие бизнеса»»; «65% было у ООО «Гурбей Золото»
  Игоря Елисеева и экс-гендиректора девелоперской группы «Самолет»
  Антона Елистратова» (тот же Елистратов, что и в исходной карточке);
  «По итогам 2024 года «Техсервис» сократил выручку на 6% до 1,47 млрд
  рублей, и получил чистый убыток в размере 517,98 млн рублей против
  прибыли 387,12 млн рублей годом ранее».
- ircity.ru/text/economics/2025/10/09/76066079/ (09.10.2025): «Кто стоит
  за новым владельцем месторождений — «ИГ Геоинвест» — пока неизвестно.
  В компании не раскрывают своих собственников. Известно лишь, что
  возглавляет фирму Дмитрий Кравчук, ранее занимавшийся торговым
  бизнесом в Санкт-Петербурге»; причина продажи — «низкое содержание
  золота в руде, проблемы с инфраструктурой и большие затраты».

НЕ ВНЕСЕНО: сама сделка «Ростех → «ИГ Геоинвест»» (октябрь 2025) — это
ОТДЕЛЬНАЯ, более поздняя и совсем другая по составу сторон сделка, для
неё в базе нет карточки; решение о заведении — за приточной рутиной,
вынесено в «Известные проблемы» CLAUDE.md.

`status`/`buyer`/`title` карточки НЕ тронуты — интерес «Росатома» 2022
года не подтверждён ни закрытым, ни прямо опровергнутым ни одним
источником, только полным отсутствием дальнейших упоминаний; решение,
менять ли статус на «Не состоялась», оставлено человеку.

Запуск: python3 pipeline/fix_techservis_rosatom_never_closed.py
        python3 pipeline/fix_techservis_rosatom_never_closed.py --write
"""
import json
import os
import sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
PATH = os.path.join(ROOT, 'static', 'data', 'deals_promoted.json')

DEAL_ID = 'g4cb8fc20'

OLD_ECO_CONTEXT = '—'
NEW_ECO_CONTEXT = (
    'Интерес «Росатома» 2022 года ни разу больше не упоминается ни в одном '
    'источнике. В октябре 2025 года «Ростех» и его партнёры («Гурбей '
    'Золото» Игоря Елисеева и Антона Елистратова, владевшие 65%, и сама '
    '«РТ-Развитие бизнеса» с 35%) продали 100% «Техсервиса» ранее не '
    'известной на рынке компании «ИГ Геоинвест» — причиной названы низкое '
    'содержание золота в руде, проблемы с инфраструктурой и убыток '
    '518 млн ₽ за 2024 год при выручке 1,47 млрд ₽.'
)


def main(write=False):
    with open(PATH, encoding='utf-8') as f:
        data = json.load(f)
    deal = next(d for d in data['deals'] if d['id'] == DEAL_ID)

    assert deal['eco']['context'] == OLD_ECO_CONTEXT

    print('=== eco.context: станет ===')
    print(NEW_ECO_CONTEXT)

    if write:
        deal['eco']['context'] = NEW_ECO_CONTEXT
        with open(PATH, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=1)
        print('\nЗаписано.')
    else:
        print('\nСухой прогон — ничего не записано. Запустите с --write.')


if __name__ == '__main__':
    main(write='--write' in sys.argv)
