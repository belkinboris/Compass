# -*- coding: utf-8 -*-
"""Месячная очередь (REVISION_BRIEF, третий уровень), карточка
`g9cf902c6` («VK увеличила пакет в Нота-РТК до контрольного», закрыта,
2023) — дочитывание уточнило, чем конкретно занимается предмет сделки
(упоминание «РТ Лабс» в `eco.context` — это связанная сторона по
продукту, а не путаница), и добавило финансы сразу после сделки.

Проверено (по докладу саб-агента, дословные цитаты):
- kommersant.ru/doc/7196211 (более поздняя статья, но подтверждает
  природу продукта): «Издателем выступает Минцифры, разработчиком была
  "РТ Лабс" (дочка "Ростелекома") совместно со студией Notamedia» — речь
  о приложении «Госуслуги Культура» / «Пушкинская карта». То есть «РТ
  Лабс» в `eco.context` назван не по ошибке: это продавец первого
  транша 26% И одновременно совместный разработчик того же продукта до
  сделки — конкретный продукт «Нота-РТК» был назван в самой карточке
  только описательно («мобильные приложения... программные решения»),
  теперь у него есть имя.
- akm.ru/news/vk_uvelichila_dolyu_uchastiya_v_vk_art_lab_do_75/: «Выручка:
  207 млн руб. Чистая прибыль: 29 млн руб.» — за 2023 год, первый полный
  год после сделки.

НЕ ВНЕСЕНО: (1) сумма сделки — ноль по всем 8 проверенным источникам
(АК&М, Интерфакс, Habr, Коммерсантъ, РБК, Sostav ×2); (2) юридический/
финансовый консультант — ноль по всем источникам; (3) финансы за 2022
год — ни один источник их не приводит; (4) дальнейшая судьба компании
(рост доли VK до 75% в 2025, до 100% в 2026, переименование в «ВК Арт
Лаб», ликвидация с июля 2026) — это уже другой, гораздо более поздний
сюжет с собственными датами и деталями; записано отдельно в разделе
«Известные проблемы» CLAUDE.md как материал для решения о новых
карточках, а не втиснуто в эту карточку 2023 года.

Запуск: python3 pipeline/fix_vk_nota_rtk_product_and_financials.py
        python3 pipeline/fix_vk_nota_rtk_product_and_financials.py --write
"""
import json
import os
import sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
PATH = os.path.join(ROOT, 'static', 'data', 'deals_promoted.json')

DEAL_ID = 'g9cf902c6'

OLD_ECO_SHARE = (
    'Нота-РТК занимается созданием мобильных приложений, '
    'высоконагруженных веб-сервисов и других программных решений.'
)
NEW_ECO_SHARE = (
    OLD_ECO_SHARE + ' В частности, компания разрабатывала мобильное '
    'приложение «Госуслуги Культура» для программы «Пушкинская карта» — '
    'совместно с прежним совладельцем, «РТ Лабс».'
)

OLD_ECO_TARGET_FIN = '—'
NEW_ECO_TARGET_FIN = (
    'По итогам 2023 года — первого полного года после сделки — выручка '
    'компании составила 207 млн ₽, чистая прибыль — 29 млн ₽.'
)


def main(write=False):
    with open(PATH, encoding='utf-8') as f:
        data = json.load(f)
    deal = next(d for d in data['deals'] if d['id'] == DEAL_ID)

    assert deal['eco']['share'] == OLD_ECO_SHARE
    assert deal['eco']['target_fin'] == OLD_ECO_TARGET_FIN

    print('=== eco.share: станет ===')
    print(NEW_ECO_SHARE)
    print('\n=== eco.target_fin: станет ===')
    print(NEW_ECO_TARGET_FIN)

    if write:
        deal['eco']['share'] = NEW_ECO_SHARE
        deal['eco']['target_fin'] = NEW_ECO_TARGET_FIN
        with open(PATH, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=1)
        print('\nЗаписано.')
    else:
        print('\nСухой прогон — ничего не записано. Запустите с --write.')


if __name__ == '__main__':
    main(write='--write' in sys.argv)
