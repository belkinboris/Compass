# -*- coding: utf-8 -*-
"""Месячная очередь (REVISION_BRIEF), карточка gadd238c3 (ГК «Фармасинтез»/
АО «Рестер»): поле `date` несло «2025-07-07», хотя год сделки, по всей
видимости, 2026-й.

Доказательства (все проверены прямым WebFetch, не по сниппетам поиска):
- Коммерсантъ (https://www.kommersant.ru/doc/8799527) — дата публикации
  «09.07.2026», текст называет выставку «Иннопром-2026» дважды; статья
  цитирует показатели «Фармасинтеза» ЗА 2025 ГОД КАК УЖЕ ЗАВЕРШЁННЫЙ —
  «В 2025 году выручка группы составила 50.5 млрд руб.» (см. AKM ниже) —
  что осмысленно только если сама статья вышла в 2026 году.
- Ведомости, пресс-релиз (URL-путь
  .../2026/07/07/farmasintez-priobrel-70-...) — год в самом адресе
  страницы «2026», текст также называет выставку «Иннопром-2026».
- AKM.RU — дата публикации «10 июля 2026».
- ФармМедПром (pharmprom.news) — заголовок прямо называет «Иннопром-2026»,
  дата публикации «07.07.2026».
- Собственный `added` карточки — «2026-07-23», то есть через 16 дней
  после 2026-07-07 (согласуется со свежей новостью), а не через год с
  лишним после 2025-07-07 (что было бы странно для потока свежих сделок).

Единственное упоминание «Иннопром-2025» нашлось в ДРУГОЙ статье
Коммерсанта (doc/8830342, тоже датированной 2026 годом — «20.07.2026»,
про последующую встречу в Удмуртии) — это внутреннее противоречие того
источника с самим собой (см. CLAUDE.md, «Источник может расходиться сам
с собой»): один источник против четырёх, и число «2026» подтверждается
у четырёх НЕЗАВИСИМО, включая через URL и через контекст (показатели
2025 года как уже известные).

Не через review.py: `date_is_supported()` в review.py сознательно не
умеет переносить сделку в другой год (см. CLAUDE.md) — это решение
принимается отдельным скриптом с явным assert на исходное состояние.
`law.struct` карточки (дословная цитата ИМЕННО из doc/8830342, где стоит
«Иннопром-2025») не трогается — это честная передача цитаты источника,
который сам ошибся в этом месте, а не факт, который мы утверждаем от
своего имени.
"""

import json
import os

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
PATH = os.path.join(ROOT, 'static', 'data', 'deals_promoted.json')

CARD_ID = 'gadd238c3'
OLD_DATE = '2025-07-07'
NEW_DATE = '2026-07-07'


def main(write=False):
    data = json.load(open(PATH, encoding='utf-8'))
    deal = next(d for d in data['deals'] if d['id'] == CARD_ID)

    assert deal['date'] == OLD_DATE, f"date: неожиданное значение {deal['date']!r}"

    print(f"{CARD_ID} date: {OLD_DATE} -> {NEW_DATE} "
          "(четыре независимых источника называют «Иннопром-2026»)")
    deal['date'] = NEW_DATE

    if write:
        with open(PATH, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=1)
        print("ЗАПИСАНО")
    else:
        print("Сухой прогон. Запись — с --write.")


if __name__ == '__main__':
    import sys
    main(write='--write' in sys.argv)
