# -*- coding: utf-8 -*-
"""Дневная очередь качества, 12 сентября 2026 — карточка `g1536c7fc`
(Райффайзенбанк вошёл в капитал разработчика ПО «Точный код»). Полный
обыск саб-агентом: единственный источник (Frank Media) уже содержал
больше фактов, чем было перенесено при приёмке — мотив сделки и точный
профиль компании остались непрочитанными до конца. Дубль на mail.ru
подтверждает те же цифры дословно.

НЕ ВНЕСЕНО: сумма инвестиции/оценка компании — не названа ни в одном
источнике; какое именно юрлицо инвестировало (АО «Райффайзенбанк» или
головной RBI) — источники пишут просто «Райффайзенбанк», не уточняя;
`accepted_notes` уже фиксирует это как решённый вопрос (профиль — общий
брендовый, отдельного юрлица в базе нет) — не переоткрывается. Имена и
биографии пяти учредителей по отдельности — WebSearch не нашёл ничего
релевантного (только однофамильцы); источники называют лишь общую фразу
про часть из них.

eco.rationale (было «—», по Frank Media, дословно): мотив — требование
законодательства об отечественном ПО на объектах критической
информационной инфраструктуры (КИИ).

law.struct (было «—»): профиль компании и её учредителей — уточняет, что
«Точный код» делает ПО именно для КИИ финансового сектора (точнее, чем
общее «для финансового сектора»), и что часть учредителей ранее работала
в Райффайзенбанке.

Запуск:
    python3 pipeline/fix_daily_queue_2026_09_12_raiffeisen_tochny_kod.py            # сухой прогон
    python3 pipeline/fix_daily_queue_2026_09_12_raiffeisen_tochny_kod.py --write    # запись
"""
import json
import sys

DATA = 'static/data/deals_promoted.json'

NEW_ECO_RATIONALE = (
    'Участие в компании позволит Райффайзенбанку выполнить требования '
    'законодательства об использовании отечественного программного '
    'обеспечения на значимых объектах критической информационной '
    'инфраструктуры.'
)

NEW_LAW_STRUCT = (
    '«Точный код» занимается разработкой программного обеспечения для '
    'критической информационной инфраструктуры (КИИ) финансового сектора '
    'и станет самостоятельной российской компанией. Часть из пяти '
    'учредителей, судя по открытым данным, ранее работала в '
    'Райффайзенбанке, остальные — независимые инвесторы с опытом работы '
    'в финансовой отрасли.'
)

NEW_SRC = [
    ['finance.mail.ru', 'https://finance.mail.ru/article/'
                        'rajffajzenbank-priobrel-dolyu-v-razrabotchike-'
                        'softa-dlya-finsektora-69227387/'],
]


def main(write=False):
    with open(DATA, encoding='utf-8') as f:
        data = json.load(f)
    by_id = {d['id']: d for d in data['deals']}
    d = by_id['g1536c7fc']

    assert d['eco']['rationale'] == '—', 'eco.rationale уже занято: %r' % (d['eco']['rationale'],)
    assert d['law']['struct'] == '—', 'law.struct уже занято: %r' % (d['law']['struct'],)
    urls = {s[1] for s in d['src']}
    for name, url in NEW_SRC:
        assert url not in urls, 'источник уже добавлен: %s' % url

    print('g1536c7fc: заполняю eco.rationale (требование закона о ПО на '
          'объектах КИИ), law.struct (профиль «Точного кода» и часть '
          'учредителей — экс-сотрудники Райффайзенбанка), добавляю источник '
          '(finance.mail.ru)')

    if not write:
        print('Сухой прогон. Запись — с ключом --write.')
        return

    d['eco']['rationale'] = NEW_ECO_RATIONALE
    d['law']['struct'] = NEW_LAW_STRUCT
    d['src'].extend(NEW_SRC)

    with open(DATA, 'w', encoding='utf-8') as f:
        json.dump(data, f, ensure_ascii=False, indent=1)
    print('Записано.')


if __name__ == '__main__':
    main('--write' in sys.argv)
