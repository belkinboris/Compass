# Вехи, статусы, диалог в консоли, отсев сырья — инструкция на внедрение

Спецификация от 21 августа 2026 (архитектура согласована с владельцем в
сессии; исполнять по частям, каждая — отдельный коммит с тестами,
семантическим diff и Playwright там, где трогается интерфейс). Часть 1
вех уже сделана и запушена: `review.py --milestone <id> <kind> --write`
кладёт в событие `newsworthy: true` и `snapshot`, страница этапа
показывает панель «Карточка на момент этого этапа» (см. раздел «Новый
этап сделки — веха» в REVISION_BRIEF.md). Ниже — что дальше.

ПОРЯДОК РАБОТ (по горящести, не по размеру) — ВСЕ ЧЕТЫРЕ СДЕЛАНЫ И
ЗАПУШЕНЫ 22 августа 2026:
1. D — предварительный отсев сырья (владелец вчера вручную отбросил ~100
   мусорных «сомнительных»; самая острая боль). ГОТОВО:
   `pipeline/ingest/raw_screen.py`, шаг притока.
2. C — подтверждение и ответ на заметки в консоли (маленькое, сильно
   улучшает опыт основателей). ГОТОВО: мгновенный штамп в `main.py`,
   `read_notes.py --reply`, шаг притока.
3. B — цветные плашки статусов (час работы). ГОТОВО: `kindClass()` в
   static/index.html, применено везде, где рендерится статус.
4. A — вехи в ленте и постах (самое крупное, трогает главную ленту —
   последним, с полной проверкой). ГОТОВО двумя коммитами: данные + лента,
   затем канал (см. раздел «Канал» ниже — три отступления от буквы этого
   файла, каждое обосновано и проверено).

---

## A. Вехи в ленте сайта и в канале

РЕШЕНИЕ ЗАФИКСИРОВАНО: веха — НЕ вторая карточка в `deals[]`, а запись
ленты, указывающая на ту же карточку. Каждый случай «одна сделка — две
карточки» в истории проекта признавался дефектом и сливался скриптом
(Астра, ВТБ/RWB); на отдельных карточках ломаются счётчики (профили
компаний, аналитика, TOTAL_DEALS) и дедупликация притока. Читательский
сценарий владельца при этом сохраняется целиком: своя строка в ленте со
своим заголовком и датой вехи, свой пост в канале, своя страница момента
(страница этапа из части 1).

Данные:
- `events[].headline` — новое поле: человеческий заголовок вехи
  («Правкомиссия одобрила покупку Компании А»). Пишется рутиной при
  создании вехи, проходит консоль (см. ниже), правится ответом-текстом
  как обычный пост. `dealEvents()` уже читает `e.headline` в
  `historicalTitle` — проверить и использовать.
- Закрытый список постворси-видов: `approval`, `closed`, `cancelled`
  (`signed` — обсудить с владельцем отдельно, в v1 не включать).
  Существенное изменение цены/периметра видом события не является — в v1
  не веха.

Создание вехи (шаг почасовой рутины притока, после enrich):
- Если `enrich.py` добавил к карточке, УЖЕ находящейся в базе на сайте,
  событие постворси-вида (`review.POSTWORTHY_MILESTONE_KINDS` —
  `approval`/`closed`/`cancelled`) — рутина тем же прогоном: вычитка
  карточки (Шаг 0 REVISION_BRIEF) →
  `review.py --milestone <id> <kind> "<заголовок>" --write` (порядок
  аргументов — `--milestone` первым; снимок берётся ДО обогащения более
  поздними фактами — порядок важен, см. предупреждение в REVISION_BRIEF;
  заголовок пишется этим же вызовом, третьим аргументом, реализовано
  22 августа) → черновик поста вехи в консоль.
- Если карточка ещё в pending — веха не нужна: карточка сама ещё новость.
- ДЕДУП СОБЫТИЙ ОБЯЗАТЕЛЕН ДО ЭТОГО ШАГА: `enrich.py` не должен
  добавлять второе событие того же `kind` (два издания об одном
  одобрении ФАС = одно событие). Проверить, есть ли guard; нет — добавить
  с тестом. Ключ дедупа вехи: стабильный `event.id` (`<deal_id>-<kind>`,
  часть 1 уже присваивает).

Лента сайта (`static/index.html`):
- Сборщик ленты объединяет карточки и вехи: веха = {deal, event} с
  `newsworthy` и `headline`, дата — дата события. Отдельный видимый
  признак (чип «Веха · Согласование» / «Веха · Закрытие»).
- Клик по вехе → `#/deal/<id>` (актуальная карточка; блок «Ход сделки»
  уже сам появляется при 2+ этапах, ранние этапы уже кликабельны).
- ЗНАМЕНАТЕЛИ НЕ ТРОГАТЬ: TOTAL_DEALS(), аналитика, счётчики профилей
  считают ТОЛЬКО карточки. Тест: пометка вехи не меняет число сделок на
  главной и в аналитике.
- `isDealShownOnSite()` применяется к родительской карточке вехи.

Канал — СДЕЛАНО 22 августа, с тремя отступлениями от буквы этого файла,
каждое — по конкретной, проверенной причине:
- Черновик вехи в консоль: «📌 [веха <deal_id>~<kind>] — НА КАНАЛ, на
  проверку» + `format_post.render_milestone()`, с теми же кнопками, что у
  поста (пост в канал / без поста, вердикты `post_ok`/`post_no` — те же
  имена, что у обычного поста, не отдельные). Отдельный скрипт
  `pipeline/ingest/send_milestone_drafts.py`, не встроено в
  `send_drafts.py`: три существующих типа сообщений уже устроены вокруг
  ОДНОЙ сделки, а веха — вокруг ОДНОГО СОБЫТИЯ; ввинчивать четвёртый тип в
  и без того плотный `build_plan()` рискованнее для уже работающего потока
  карточек/сырья, чем маленький параллельный скрипт с той же консолью.
  Callback-регэксп в `main.py` (`mod:...`) расширен на `[\w~-]{1,60}`
  (было `[\w-]{1,40}`) — `deal_id` в `ModerationDecision` тоже расширен
  с 40 до 80 (миграция `ALTER COLUMN TYPE` — только на Postgres,
  диалект-независимо, тот же приём инспектора, что у `chat_id`/
  `reply_message_id` из раздела C).
- Молчание сутки = веха выходит — считается от `event['milestone_drafted_at']`
  (ставит `send_milestone_drafts.py`), применяет `send_telegram.py`'s
  `plan_milestones()` тем же прогоном, что и живые посты — отдельного шага
  в рутине публикации заводить не пришлось. `check_post.py` вычитывает
  текст вехи тем же вызовом `check()`, без изменений в самом check_post.py.
- ОТСТУПЛЕНИЕ 1 — «двойное уведомление» и `should_notify`. Проверено:
  `should_notify()` (format_post.py) считается только внутри `enrich.py` и
  пишется в `data/inbox/updates/`, но НИ ОДИН реальный отправитель этот
  флаг не читает — `send_telegram.py`'s `load_today_updates()` берёт только
  `changes`, не `notify`. `should_notify` — код без вызывающего (родня уже
  записанного класса в CLAUDE.md). А `editMessageText` в принципе не
  уведомляет подписчиков канала — это свойство Bot API, не наше решение.
  То есть «двойной сигнал», которого боялся этот файл, структурно уже не
  мог случиться: живой пост тихо получает «⟳ Обновлено», новый пост-веха
  (sendMessage) уведомляет один раз. Убирать «исключение из send_telegram»
  было НЕЧЕГО — его там и не было (`send_telegram.py` не содержит
  спецкейса на `status=='Закрыта'`); `format_post.py`'s `should_notify`
  оставлен как есть — трогать мёртвый код не по этой задаче было бы
  случайной правкой не туда.
- ОТСТУПЛЕНИЕ 2 — `telegram_posts[deal_id]` НЕ расширен вложенным
  `stage_posts`. Проверено: значение этого поля — либо `None`, либо голое
  число (message_id), это закреплено тестом (`test_main_sends_backlog_
  entry_as_fresh_post_when_new_fact_appears`, сравнение `== 777`) и читается
  `main.py`'s `_ops_numbers()` (`sum(1 for v in posts.values() if v)`).
  Обернуть в словарь — рискованная миграция формы поля ради вех, когда у
  события и так уже есть свой стабильный `id`. Вместо этого — отдельный
  словарь верхнего уровня `data['telegram_milestones']` = `{event_id:
  {message_id, at}}`: тот же функциональный дедуп, без миграции.
- ОТСТУПЛЕНИЕ 3 — двухфазный `--consume` (как у approve.py) НЕ понадобился.
  approve.py консуммирует решение сайту отдельным, поздним шагом ПОСЛЕ git
  push, потому что дедуп КАРТОЧКИ держится на факте консумации (`decided_raw`)
  — применить решение дважды значило бы завести вторую карточку. У вехи
  дедуп держится на `telegram_milestones[event_id]` — это git-нативное,
  ЛОКАЛЬНОЕ состояние, а не подтверждение сайту; повторная выборка того же
  решения безопасна в любом порядке, поэтому `send_telegram.py` консуммирует
  решения вех сразу, в том же прогоне, без отдельного шага в рутине.

Тесты: инвариант test_data «каждая веха ссылается на существующее
событие существующей карточки и несёт headline» — готово
(`test_newsworthy_milestones_are_well_formed`); UI-тест «веха в ленте
открывается, счётчик сделок не изменился» — готово
(`test_milestone_appears_in_feed_without_moving_the_deal_count`);
ingest-тест «второе событие того же kind не добавляется» — готово, ЗАМЕРЕНО
поведение уже существующего guard'а enrich.py (дедуп по (kind, date), а не
по одному kind — «два согласования» в разные даты остаются легитимным
случаем, задокументированным в самом коде); постовый тест «на событие с
вехой не уходит двойное уведомление» — не понадобился в буквальном виде
(см. «Отступление 1» выше: двойного уведомления структурно не существует),
вместо него — `test_main_sends_an_approved_milestone_and_records_dedup`
(веха уходит отдельным sendMessage, живой пост не трогается).

---

## B. Цветные плашки статусов

НОВЫЙ СТАТУС В ДАННЫЕ НЕ ЗАВОДИТЬ. «Обсуждается» уже покрывает и
переговоры, и планы («Лента может купить») — отдельное значение
«Планируется» трогало бы закрытый список статусов, правила притока и
тесты ради слова на экране. Решение владельца — различать цветом:
- «Обсуждается» — синяя плашка;
- «Подписана», «Согласование получено» — янтарная/промежуточная;
- «Закрыта» — зелёная (как сейчас);
- «Не состоялась» — красная/серая (как сейчас).
Применить ВЕЗДЕ, где рендерится статус: лента, шапка карточки, страница
этапа, компактная строка «Ход сделки». Проверить контраст в обеих темах
(тёмная — отдельно). Playwright: по одной карточке каждого статуса, обе
темы, 360/1280.

---

## C. Диалог в консоли (заметки с ответом)

УЖЕ РАБОТАЕТ (сказать владельцу ничего не ломая): ответ РЕПЛАЕМ на
сообщение бота [сырьё …]/[карточка …] сохраняется заметкой
(verdict='note'), почасовой приток читает её через
`pipeline/ingest/read_notes.py` с контекстом и применяет через
review.py, затем `--consume`. Чего нет — обратной связи. Добавить два
уровня:

1. МГНОВЕННОЕ ПОДТВЕРЖДЕНИЕ (вебхук, `main.py`): получив заметку,
   дописать в исходное сообщение строку «— 💬 Заметка принята (Имя),
   рутина ответит после чтения» — тот же приём, что «— ✅ Одобрено».
2. СОДЕРЖАТЕЛЬНЫЙ ОТВЕТ РУТИНЫ: после применения заметки рутина обязана
   ответить в группе реплаем на ТО ЖЕ сообщение: что сделала, что нашла,
   с чем не согласна и почему (честно, коротко). Для этого:
   - в `ModerationDecision` добавить колонки `chat_id` и
     `reply_message_id` (id сообщения бота, на которое ответили;
     вебхук видит его в `message.reply_to_message.message_id`).
     МИГРАЦИЯ: через инспектор SQLAlchemy + обычный ADD COLUMN, БЕЗ
     `IF NOT EXISTS` — урок «ALTER TABLE … IF NOT EXISTS — синтаксис
     Postgres, не SQLite» в CLAUDE.md; проверить на ОБЕИХ базах.
   - `read_notes.py` отдаёт эти поля; рутина шлёт ответ через тот же
     Telegram API (`reply_to_message_id`).
   - в промпт притока (пересоздание триггера — см. ниже) добавить:
     каждая потреблённая заметка получает ответ-реплай.

---

## D. Предварительный отсев сырья («сомнительных») перед консолью

БОЛЬ: владелец вчера вручную отбросил ~100 мусорных «сомнительных»
(свадебный «выкуп», макроэкономика, потребительские гайды). Механический
фильтр это не возьмёт — нужен отсев моделью, благо приток теперь
почасовой и модель уже в контуре.

Новый шаг рутины притока между promote и send_drafts. Контракт:
- Скрипт `pipeline/ingest/raw_screen.py`:
  - без аргументов: печатает свежие неотправленные сомнительные
    (id | заголовок | причины ворот | ТОП-СОВПАДЕНИЕ С БАЗОЙ через
    matcher: id карточки + сила сигнала — для распознавания «это
    дополнение, а не новая»);
  - `--drop <id> <id> … --reason "…" --write`: помечает
    `decided_raw[id]='auto-drop'` + `raw_titles` (не покажутся никогда);
  - `--enrich <id>=<deal_id> --write`: помечает «это дополнение к
    существующей карточке» — сырьё не идёт в консоль, а встаёт в
    очередь обогащения (рутина читает статью и несёт факты в указанную
    карточку через review.py; случай Alumni Partners/«Полекс» —
    объявление консультанта об уже известной сделке, самое ценное — имя
    консультанта в law.adv).
- Рутина (модель) судит каждый пункт по трём классам:
  1. МУСОР УВЕРЕННО → --drop;
  2. похоже на сделку ИЛИ непонятно → в консоль, как сейчас;
  3. совпадает с существующей карточкой → --enrich.
  ПРИ СОМНЕНИИ — ПОКАЗЫВАТЬ. Правило дома: терять сделку молча дороже,
  чем показать человеку лишнее.

  СПИСОК КЛАССОВ «МУСОР УВЕРЕННО» — ИЗМЕРЕН, А НЕ НА ГЛАЗ (21 августа,
  118 реально восстановленных заголовков из истории `decided_raw`/
  `raw_titles`, drop-решения владельца + все 12 take для контроля нулевых
  ложных срабатываний):
  - свадьбы, похороны/кладбища, личный быт («Купить место на кладбище
    можно через Госуслуги», «Выкуп, каравай и икона в банкетном зале»);
  - макроэкономика/госфинансы без конкретной сделки (курсы, статистика,
    межгосударственная сырьевая торговля вроде «Россия купила у Бразилии
    какао-бобы», комментарии ЦБ);
  - потребительские гайды и советы («риски при покупке квартиры»,
    опросы вроде «Больше половины владельцев iPhone готовы купить
    Android»);
  - спорт (трансферы игроков, результаты матчей — «"Барселона" купит у
    "Манчестер Сити" Родри», «"Фенербахче" и "Лион" сыграли вничью»);
  - политика/геополитика без сделки, включая метафорическое
    «купил»/«продал» («Пашинян не продал абрикосы европейцам», «Белый
    дом перешёл к стратегии удушения Ирана»);
  - НОВОЕ, не было в списке владельца: мошенничество/криминал, где
    «покупка»/«продажа» — способ обмана, не предмет статьи («Мошенники
    начали продавать фейковые билеты», «кража данных карт под видом
    продажи формы»);
  - НОВОЕ: вакансии («Вакансия Менеджер проектов M&A в Т-банк»);
  - НОВОЕ: вебинары и образовательный контент про M&A вообще, не про
    конкретную сделку («Личный фонд в структуре сделки — на вебинаре...»);
  - НОВОЕ: дайджесты и подборки — шире, чем текущий `DIGEST` в
    classify.py: не только «топ-N», но и «Крупнейшие M&A-сделки в
    российском АПК за год», «Продажа ТРЦ и новые деревья: главное в
    Кургане за 13 августа».
  ПРОВЕРЕНО НА СЕБЕ: ни один из 12 заголовков `take` не подходит ни под
  один из этих классов — ложных auto-drop на реальных сделках при этом
  замере ноль. Список заведомо неполон (собран по ОДНОЙ партии решений,
  как и любой словарь в этом проекте) — расширять по мере находок, тем
  же приёмом: замер по `decided_raw` перед тем, как добавить класс.

  ОТДЕЛЬНАЯ НАХОДКА ТОГО ЖЕ ЗАМЕРА: одна и та же несостоявшаяся история
  («Рижский вокзал») встретилась в отброшенном сырье ЧЕТЫРЕ раза за
  месяц с разными формулировками («не состоялась в третий раз», «снова
  не состоялась», «вновь не состоялась») — точный `raw_key()` их не
  склеивает. Это не мусор в смысле «не по теме», а ПОСТОЯННО
  ПОВТОРЯЮЩАЯСЯ НЕ-НОВОСТЬ: `raw_screen.py` показывает список одним
  экраном, и рутина, увидев несколько похожих заголовков подряд, может
  выкинуть их одним вызовом `--drop <id1> <id2> ... --reason "..."`
  (флаг уже поддерживает несколько id) — отдельного кода для склейки
  такого класса не потребовалось.
- ЗАМЕР ДО ВКЛЮЧЕНИЯ — СДЕЛАН (см. выше, цифры и заголовки — в этом
  файле, не только в докстринге скрипта, чтобы не потерять при следующей
  правке `raw_screen.py`): 0 ложных auto-drop на 12 take, список классов
  уточнён по 118 реальным drop. Из повторного профильного риска —
  границы найденных классов узкие и текстовые (конкретные слова:
  «кладбище», «свадьб», «вакансия», спортивные клубы), не «любая
  недвижимость»/«любая политика» — так же, как остальные словари
  проекта, держать узко и расширять по находкам, а не расширять формулировку
  заранее «на всякий случай».
- Отчётность: `ops_status.py приток` получает параметр `--screened <N>`
  («отсеяно автоматически: N») — ноль тоже печатается. Авто-дропы
  остаются в moderation_state (git) с причиной — аудит возможен всегда.
- Отдельный класс — СП: «Правительство одобрило создание СП X и Y»
  режется воротами за «нет предмета», хотя у СП предмета-покупки и не
  бывает. В ворота правило НЕ вносить без замера; в v1 отсев помечает
  такие «похоже на СП: стороны есть, предмет — само СП» и показывает
  человеку с этой подсказкой.
- Его ручные drop — размеченный корпус для learn.py: отдельной задачей
  прогнать по нему кандидатов на 2–3 дешёвых механических правила
  (свадебная лексика, «сколько стоят»-гайды), каждое — с замером полноты
  по своей базе (не потерять «тендерное предложение о выкупе»).

---

## Пересоздание триггера притока

Промпт чужой персистентной сессии через update_trigger НЕ редактируется
(проверено 21 августа — отказ инструмента). Порядок: delete_trigger →
create_trigger с тем же `persistent_session_id`
(session_01BhciyKvRJEoYidQzFD3QoP) и тем же cron (`20 5-18 * * *`).

ШАГИ ОТСЕВА (D) И ЗАМЕТОК (C) — УЖЕ В ПРОМПТЕ (пересоздан 22 августа,
шаги 7 и 8 текущего порядка). ШАГ СОЗДАНИЯ ВЕХИ (A) — ЕЩЁ НЕТ, добавить
следующим пересозданием: после того как шаг 9б (вычитка и запись) находит
и записывает событие постворси-вида (`review.POSTWORTHY_MILESTONE_KINDS`
— `approval`/`closed`/`cancelled`) на карточке, УЖЕ находящейся в базе
сайта (не в pending — там карточка сама ещё новость), тем же прогоном:
`review.py --milestone <id> <kind> "<заголовок>" --write` (снимок берётся
ДО дальнейшего обогащения — порядок важен, см. предупреждение в самой
докстроке `mark_milestone`), затем `python3 pipeline/ingest/
send_milestone_drafts.py --write` — отправит черновик в консоль тем же
прогоном, не дожидаясь следующего часа. Публикацию (`send_telegram.py`)
трогать НЕ пришлось — веха уже отправляется/молчание-публикуется тем же
прогоном публикации, что и живые посты (см. раздел «Канал» выше).
