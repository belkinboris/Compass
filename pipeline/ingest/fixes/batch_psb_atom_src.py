# -*- coding: utf-8 -*-
"""ПСБ/«Атом» (`gcaa03820`) — седьмая находка владельца 18 августа, отдельная
от `fix_psb_atom_investment_card.py`: карточка несла в `eco.val`/`eco.rationale`/
`eco.context` факты, дословно взятые из статьи Коммерсанта («Ъ» упомянут в
тексте трижды — «подробности об этом "Ъ" обнаружил…», «Источник "Ъ"
рассказывал…»), а в `src` при этом стоял только телеграм-канал.

Это НЕ тот же класс, что 76 карточек, записанных в CLAUDE.md как
неоднозначные (атрибуция ВНУТРИ уже прочитанного источника, где добавлять
чужого в `src` было бы неверно, — см. «Источник — то, что подтверждает
факт, а не то, как о нём узнали»): здесь сам телеграм-пост, из которого
собрана карточка, приводит прямую ссылку на Коммерсанта в конце текста
(https://www.kommersant.ru/doc/8877492) — то есть это репост, а не
собственный анализ канала, и адрес первоисточника был всё это время виден,
просто не забран. Статья скачана (`fetch_article_texts.py --write`,
18 августа) и подтверждает каждый факт дословно.

Запуск: python3 pipeline/ingest/review.py --write
"""

FIXES = [
    dict(id='gcaa03820', field='src', old=None,
         new=['Коммерсантъ', 'https://www.kommersant.ru/doc/8877492'],
         quote='ПСБ приобрел 16,98% акций производителя электромобилей '
               '«Атом» АО «Кама» за 9 999 613 000 руб., сообщили в банке.',
         why='весь текст eco.val/eco.rationale/eco.context карточки взят '
             'дословно из этой статьи (в тексте трижды упомянут «Ъ» как '
             'источник фактов), а в src стоял только телеграм-репост — '
             'ссылка на Коммерсанта была в конце самого репоста, просто не '
             'забрана при разборе'),
]
