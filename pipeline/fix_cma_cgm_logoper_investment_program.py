# -*- coding: utf-8 -*-
"""Месячная очередь (REVISION_BRIEF, третий уровень), карточка
`ga2e9c5b1` («CMA CGM продала 50% российского контейнерного оператора
«Логопер» Александру Кахидзе», закрыта 12.04.2023) — дальнейшая судьба
компании и статус опциона на обратный выкуп не были отражены.

Проверено лично прямым WebFetch, Ведомости,
https://www.vedomosti.ru/business/articles/2024/07/01/1047096-logoper-vlozhit-120-mlrd-rublei,
01.07.2024: «Логистическая компания «Логопер» инвестирует до 2027 г. в
общей сложности 120 млрд руб. в рамках программы по строительству 11
наземных контейнерных терминалов»; «Из общего количества уже построены
и функционируют два терминала в Московской агломерации... по одному в
Свердловской... и Новосибирской... областях»; «гендиректор и владелец
компании Александр Кахидзе» — на середину 2024 года Кахидзе назван
ЕДИНСТВЕННЫМ владельцем, то есть опцион CMA CGM на обратный выкуп на
тот момент не реализован.

НЕ ВНЕСЕНО: реализован ли опцион позже (2025-2026) — прямых свидетельств
ни исполнения, ни официального отказа CMA CGM от опциона не нашлось;
статья о возвращении CMA CGM в порт Санкт-Петербурга (октябрь 2025,
dp.ru) упоминает историю продажи «Логопера» только как справочный фон,
без новых деталей. Финансовые показатели «Логопера» за 2022-2024 годы
и структура через холдинг «КЛК Холдинг» — встретились только в
агрегаторах ЕГРЮЛ/ФНС (checko.ru, audit-it.ru), недоступных прямому
WebFetch (403); без первичного подтверждения не вношу. Утверждения
сомнительного источника (компромат-ресурс) о залоге долей Кахидзе
Сбербанку и связях с гражданином Украины — не используются вовсе,
источник недостаточно надёжен.

Запуск: python3 pipeline/fix_cma_cgm_logoper_investment_program.py
        python3 pipeline/fix_cma_cgm_logoper_investment_program.py --write
"""
import json
import os
import sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
PATH = os.path.join(ROOT, 'static', 'data', 'deals_promoted.json')

DEAL_ID = 'ga2e9c5b1'

OLD_ECO_CONTEXT = (
    'В декабре 2017 г. гендиректор CMA CGM Родольф Сааде написал письмо '
    'президенту России Владимиру Путину, в котором сообщил о намерении '
    'компании в партнерстве с ООО «Логопер» приобрести долю в '
    '«Трансконтейнере» в размере 50% (плюс 2 акции), которые на тот '
    'момент принадлежали РЖД. Однако по итогу компания не принимала '
    'участие в аукционе, новым владельцем актива стала группа компаний '
    '«Дело» Сергея Шишкарева'
)
NEW_ECO_CONTEXT = (
    OLD_ECO_CONTEXT + '. К середине 2024 года Кахидзе оставался '
    'единственным владельцем «Логопера» (опцион CMA CGM на обратный '
    'выкуп на тот момент не реализован) и объявил об инвестпрограмме '
    'до 2027 года на 120 млрд ₽ — строительство 11 наземных '
    'контейнерных терминалов, два из которых (в Московской агломерации) '
    'уже работали, ещё по одному — в Свердловской и Новосибирской '
    'областях.'
)

NEW_SRC = [
    ['Ведомости', 'https://www.vedomosti.ru/business/articles/2024/07/01/1047096-logoper-vlozhit-120-mlrd-rublei'],
]


def main(write=False):
    with open(PATH, encoding='utf-8') as f:
        data = json.load(f)
    deal = next(d for d in data['deals'] if d['id'] == DEAL_ID)

    assert deal['eco']['context'] == OLD_ECO_CONTEXT

    existing_urls = {s[1] for s in deal['src']}
    add_src = [s for s in NEW_SRC if s[1] not in existing_urls]
    new_src = deal['src'] + add_src

    print('=== eco.context: станет ===')
    print(NEW_ECO_CONTEXT)
    if add_src:
        print('\n=== src: добавится ===')
        for s in add_src:
            print(s)

    if write:
        deal['eco']['context'] = NEW_ECO_CONTEXT
        deal['src'] = new_src
        with open(PATH, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=1)
        print('\nЗаписано.')
    else:
        print('\nСухой прогон — ничего не записано. Запустите с --write.')


if __name__ == '__main__':
    main(write='--write' in sys.argv)
