# -*- coding: utf-8 -*-
"""Месячная очередь (REVISION_BRIEF, третий уровень), карточка
`g7aad7720` («ГК «Сервис-Телеком» приобрела «Опору Телеком» и «Дачу на
связи»», Закрыта) — продавец не был заполнен, хотя владельцы обеих
компаний ДО сделки уже названы в `eco.context`, а «Сервис-Телеком»
готовится к IPO.

Проверено ЛИЧНО прямым WebFetch (kommersant.ru/doc/5748009, уже в
`src`, повторно прочитан целиком): «"Опора Телеком" на 26 декабря на
31,5% принадлежит Любомиру Прокопенко и на 68,5% — АО «Сетьинвест»»;
«Дачанасвязи»: «еще 50% [владеет АО «Сетьинвест»], еще 41,5% компании у
Ольги Путьмаковой и 8,75% у Татьяны Кочновой». По условиям сделки (уже
в `eco.share`) покупатель получил 100% долей ОБЕИХ компаний — то есть
все перечисленные совладельцы вышли из капитала целиком; предыдущая
запись FIXES сознательно не заполняла `seller`, потому что слова
«продал» в статье нет буквально, но при полном (100%) переходе долей
это не требуется — прежние совладельцы и есть продавцы по определению
самой сделки.

- cableman.ru/content/servis-telekom-provodit-reorganizatsiyu-pered-
  vozmozhnym-ipo (12.02.2025): «к АО присоединяются четыре ООО:
  "Сервис-телеком", "Техноресурс", "Линк девелопмент" и "Инфраструктурные
  инвестиции"»; реорганизация — «подготовка к возможному IPO», компания
  оценивается в 30 млрд ₽, «в ходе IPO компания может привлечь 3–5 млрд
  ₽ заемных средств».

НЕ ВНЕСЕНО: создание СП «СТ-нацпроект» (август 2024, vedomosti.ru) —
отдельное, более узкое корпоративное событие, не так прямо относящееся
к судьбе именно этой сделки, оставлено за пределами карточки.

Поле `eco.context` уже прошло вычитку (`proofread_absorbed`) — за
основу слияния взят ТЕКУЩИЙ текст поля, а не старый `new` записи FIXES
(`pipeline/ingest/fixes/batch_d_2022.py`); сама запись обновлена тем же
скриптом.

Запуск: python3 pipeline/fix_servis_telekom_seller_and_ipo_prep.py
        python3 pipeline/fix_servis_telekom_seller_and_ipo_prep.py --write
"""
import json
import os
import sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
PATH = os.path.join(ROOT, 'static', 'data', 'deals_promoted.json')

DEAL_ID = 'g7aad7720'

OLD_SELLER = None
NEW_SELLER = (
    'В «Опоре Телеком» — Любомир Прокопенко и АО «Сетьинвест»; в «Даче '
    'на связи» — АО «Сетьинвест», Ольга Путьмакова и Татьяна Кочнова'
)

OLD_ECO_CONTEXT = (
    'На 26 декабря 31,5% компании принадлежало Любомиру Прокопенко, '
    '68,5% — АО «Сетьинвест»; конечные владельцы «Сетьинвеста» — Максим '
    'Родионов и Денис Сказов, это следует из пояснительной записки к '
    'бухгалтерской отчётности за 2021 год. «Сетьинвест» владеет и 50% в '
    'ООО «Дачанасвязи», ещё 41,5% этой компании — у Ольги Путьмаковой, '
    '8,75% — у Татьяны Кочновой.'
)
NEW_ECO_CONTEXT = (
    OLD_ECO_CONTEXT + ' В феврале 2025 года «Сервис-Телеком» начал '
    'реорганизацию (присоединение четырёх ООО к головному АО) в рамках '
    'подготовки к возможному IPO — компания оценивалась в 30 млрд ₽, в '
    'ходе размещения планировалось привлечь 3–5 млрд ₽.'
)


def main(write=False):
    with open(PATH, encoding='utf-8') as f:
        data = json.load(f)
    deal = next(d for d in data['deals'] if d['id'] == DEAL_ID)

    assert deal.get('seller') == OLD_SELLER
    assert deal['eco']['context'] == OLD_ECO_CONTEXT

    print('=== seller: станет ===')
    print(NEW_SELLER)
    print('\n=== eco.context: станет ===')
    print(NEW_ECO_CONTEXT)

    if write:
        deal['seller'] = NEW_SELLER
        deal['eco']['context'] = NEW_ECO_CONTEXT
        with open(PATH, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=1)
        print('\nЗаписано.')
    else:
        print('\nСухой прогон — ничего не записано. Запустите с --write.')


if __name__ == '__main__':
    main(write='--write' in sys.argv)
